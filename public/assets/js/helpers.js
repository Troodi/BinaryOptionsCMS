export async function makeApiRequest(o){try{return(await fetch(window.location.protocol+"//"+window.location.host+`/${o}`)).json()}catch(o){throw new Error(`CryptoCompare request error: ${o.status}`)}}export function generateSymbol(o,t,r){const e=`${t}/${r}`;return{short:e,full:`${o}:${e}`}}export function parseFullSymbol(o){const t=o.match(/^(\w+):(\w+)\/(\w+)$/);return t?{exchange:t[1],fromSymbol:t[2],toSymbol:t[3]}:null}
